# Local evaluation dependencies
inspect-ai>=0.3.0
inspect-evals
openai

# Local GPU evaluation (optional, GPU required)
# These are auto-installed via PEP 723 headers when using `uv run`
# Uncomment if installing manually:
# lighteval[accelerate,vllm]>=0.6.0
# vllm>=0.4.0
# torch>=2.0.0
# transformers>=4.40.0
# accelerate>=0.30.0
